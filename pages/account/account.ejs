<section id="mainSection">
    <div class="row">
        <div class="col-md-12 mt-5">
            <div class="card rounded-0 border-0">
                <div class="card-header rounded-0">
                    <h5 class="card-title text-white text-center" id="username"><%= data.user.Name %></h5>
                </div>
                <div class="card-body">
                    <ul class="list-group">
                        <li class="list-group-item">
                            <span data-trans="wordUserId">User Id</span> :  <%= data.user.Id %>
                        </li>
                        <li class="list-group-item">
                            Email: <%= data.user.Email %>
                        </li>

                        <li class="list-group-item">
                            Status :  
                            <% if (data.user.isBanned) { %>
                                <span data-trans="wordBanned">Banned</span>
                            <% } %>
                            <% if (data.user.isMuted) { %>
                                <span data-trans="wordMuted">Muted</span>
                            <% } %>
                            <% if (!data.user.isMuted || !data.user.isBanned) { %>
                                Normal
                            <% } %>
                        </li>
                        
                      </ul>
                    <div class="card rounded-0 border-0">
                        <div class="card-header rounded-0">
                            <h5 class="card-title text-white text-center" id="username">Characters</h5>
                        </div>
                        <div class="card-body">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th scope="col" data-trans="wordName">Name</th>
                                        <th scope="col" data-trans="wordClass">Class</th>
                                        <th scope="col">Map</th>
                                        <th scope="col">Action</th>
                                    </tr>
                                </thead>
                                <tbody id="playersList">
                                    <% data.characters.forEach(function(character) { %>
                                    <tr>
                                        <td><%= character.Name %>@<%= data.user.Name %></td>
                                        <td><%= character.ClassName %></td>
                                        <td><%= character.MapName %></td>
                                        <td>
                                            <a class="btn btn-primary text-decoration-none" href="/player/<%= character.Name %>" data-navigo data-trans="seeCharacter">
                                                See character
                                            </a>
                                        </td>
                                    </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</section>

